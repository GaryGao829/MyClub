<?xml version="1.0" encoding="UTF-8"?>
<rs:sqlResource xmlns:rs="http://restsql.org/schema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://restsql.org/schema SqlResource.xsd ">
	<query>
	select activity.activity_id, users.last_name, users.first_name, users.email
      from
      activity
      left outer join usercampaign on usercampaign.activity_id = activity.activity_id
      left join users on
      usercampaign.user_id = users.user_id
	</query>
	<metadata>
		<database default="myclub" />
		<table name="activity" role="Parent" />
		<table name="users" role="Child" />
		<table name="usercampaign" role="Join" />
	</metadata>
</rs:sqlResource>